# 인프라 규약

## 패키지 구조
com.example.{도메인} 하위에 엔티티, 리포지토리, 서비스, 컨트롤러를 둔다.

## 네이밍
테이블명과 컬럼명은 snake_case를 사용한다.
API 경로는 복수형 명사를 사용한다. (예: /api/todos)
JSON 필드명은 camelCase를 사용한다.

## 기타
모든 엔티티는 생성일시를 가진다.
ID는 Long 타입 자동 생성을 사용한다.
