# 응답 규약

## 성공 응답
정상 처리된 요청은 데이터를 직접 반환한다.
목록 조회는 배열로 반환한다.
생성 성공은 201, 그 외 성공은 200을 반환한다.

## 에러 응답
에러가 발생하면 다음 형태로 반환한다:
- status: HTTP 상태 코드
- message: 사람이 읽을 수 있는 에러 메시지

존재하지 않는 리소스 요청은 404를 반환한다.
잘못된 입력은 400을 반환한다.
서버 내부 오류는 500을 반환한다.
